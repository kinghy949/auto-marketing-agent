# 复制为 .env 后填入真实值。.env 已在 .gitignore 中,不会被提交。
# 生产环境通过 K8s secret / Vault 注入,不要使用本文件。

# ---- OpenAI ----
# Agents SDK 调用模型的凭证。必填。
OPENAI_API_KEY=

# 默认模型。hello 烟雾测试 / 低成本任务用;业务 agent 可按需在代码里覆盖。
OPENAI_MODEL=gpt-4.1-mini
